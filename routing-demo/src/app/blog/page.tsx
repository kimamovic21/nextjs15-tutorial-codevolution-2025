import { type Metadata } from 'next';

export const metadata: Metadata = {
  title: {
    absolute: 'Blog Page',
  },
};

const BlogPage = () => {
  return (
    <div>BlogPage</div>
  );
};

export default BlogPage;