import type { Metadata } from 'next';
import '../globals.css';

export const metadata: Metadata = {
  title: 'Routing Demo',
  description: 'Generated by create next app',
};

const MarketingLayout = ({
  children
}: {
  children: React.ReactNode
}) => {
  return (
    <html lang='en'>
      <body>
        <div>
          <h2>This is marketing layout</h2>
          <header>this is marketing header</header>
          {children}
          <footer>this is marketing footer</footer>
        </div>
      </body>
    </html>
  );
};

export default MarketingLayout;