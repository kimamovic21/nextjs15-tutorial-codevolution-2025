import Counter from './counter';

export const metadata = {
  title: 'Counter Page',
};

const CounterPage = () => {
  return (
    <div>
      <Counter />
    </div>
  );
};

export default CounterPage;